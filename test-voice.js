import fetch from 'node-fetch';

async function testVoice() {
  const businessId = "test_business_id"; 
  // We need to simulate the cookie for businessId or bypass it for testing if it's hitting the API.
  // Actually, I can just call the service directly in a script.
}
testVoice();
